<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epicodus.saladbowlcompanion">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bowl"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"
                  android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SetUpActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.RulesActivity" />
        <activity android:name=".ui.GameActivity" />
        <activity android:name=".ui.QuickModeActivity" />
        <activity android:name=".ui.CreativeModeActivity" />
        <activity android:name=".ui.TeamTransitionActivity"></activity>
    </application>

</manifest>